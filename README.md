Update day 2:
    - data is in dataclass
    - input validation
    - no global variable dependence
    - error handling with try/except
    - separated concerns across 3 files (user_profile, message, main)
    - type hints on functions

Still to do:
    - input loop (restart on bad input instead of exiting)
    - clean age error message (still shows Python's ugly message)